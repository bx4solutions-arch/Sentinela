import { EmpresaPainel } from "@/components/empresa-painel";

// Minha Empresa = o mesmo painel renderizado no hub /configuracoes?tab=perfil-empresa.
export default function EmpresaPage() {
  return <EmpresaPainel />;
}
