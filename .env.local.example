# Copie este arquivo para .env.local e preencha. NUNCA commite o .env.local.
NEXT_PUBLIC_SUPABASE_URL=https://mzaapxkdokfeyzajawzs.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=cole_aqui_a_chave_publishable

# Server-side apenas — NUNCA exponha no client, NUNCA commite:
# SUPABASE_SERVICE_ROLE_KEY=

# ── IA multi-provider (Fase A) — tudo server-side, nunca chega ao browser ──
# Provider ativo: anthropic | openai | gemini
AI_PROVIDER=anthropic
# Modelo (opcional — cada provider tem um padrão sensato se vazio)
# AI_MODEL=claude-sonnet-4-5
# Chave só do provider que você usar (as outras podem ficar vazias):
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# GEMINI_API_KEY=
